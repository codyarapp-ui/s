/**
 * مقادیر پیکربندیِ قابل استفاده در مرورگر.
 *
 * فایل `src/config/admin.ts` به `process.env` وابسته است و فقط سمت سرور کار می‌کند.
 * این فایل نسخه‌ی سبکِ سمت کلاینت است و فقط داده‌ی غیرحساس دارد.
 *
 * نکته: شماره پشتیبانی واقعی از تنظیمات سرور (`/api/settings`) خوانده می‌شود؛
 * مقدار زیر فقط تا لحظه‌ی دریافت آن تنظیمات به‌عنوان پیش‌فرض نمایش داده می‌شود.
 */

/** شماره پشتیبانی پیش‌فرض تا زمان دریافت تنظیمات از سرور. */
export const SUPPORT_PHONE = '09120947304';

/** نام نمایشی پیش‌فرض مدیر. */
export const ADMIN_DISPLAY_NAME = 'مدیر کل پلتفرم';
